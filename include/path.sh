#!/bin/bash

SNAPSHOTS_DIR=${BACKUP_ROOT}/SNAPSHOT

YEAR=`date +%Y`
DATE=`date +%m%d`

SNAP_DIR=${SNAPSHOTS_DIR}/${YEAR}/${DATE}
LOG_DIR=${BACKUP_ROOT}/LOG
LOG_FILE=${LOG_DIR}/${YEAR}/${DATE}.log
RSYNC_EXCLUDE_FILE=${BACKUP_ROOT}/exclude.ptn
